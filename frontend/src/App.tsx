import { Home } from './pages/Home'

// Only the home page exists so far — no router yet. One added when a second
// page (per docs/ARCHITECTURE.md's five routes) actually exists to route to.
export default function App() {
  return <Home />
}
